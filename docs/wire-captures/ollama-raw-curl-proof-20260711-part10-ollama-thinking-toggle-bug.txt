Ollama native /api/chat raw (curl, think:true) — minimax-m3 uses a `thinking` field inside `message`:
  {"message":{"role":"assistant","content":"","thinking":"The user is asking me to calculate..."}}
  ...then content phase: {"message":{"role":"assistant","content":"# Calculating 47 × 89..."}}

App received (debug): [OllamaChat] stream done — content=35, reasoning=211, toolCalls=3
App requested: [Provider] generate — isOllama=true, thinking=false, tools=6

FINDING (consistent across BOTH remote providers — confirmed bug):
  No thinking toggle exists for remote models → app requests thinking-off →
  server sends reasoning anyway (LM Studio: reasoning_content; Ollama: thinking field) →
  app drops it (LM Studio reasoning=0) or captures-but-doesn't-render it (Ollama reasoning=211, no thinking UI) →
  net: remote thinking NEVER renders. TOOLS WORK on both.

Thinking field ground truth (all 4):
  local gguf = inline <think>; litert = litert_thinking channel;
  openai-compat (OGAD/LM Studio) = reasoning_content; ollama native = message.thinking

=== CORRECTION (user confirmed Ollama SHOWED thinking in both messages) ===
My "consistent across both providers" was WRONG. The reasoning counts prove they DIFFER:
  - Ollama:   reasoning=211 → thinking CAPTURED + RENDERED (works, user confirmed on screen).
  - LM Studio: reasoning=0  → thinking DROPPED (no render) — THIS is the real bug.
So: parser handles Ollama native `thinking` field correctly, but drops OpenAI-compat `reasoning_content`
(LM Studio path). Toggle absence is a separate minor UX gap; Ollama renders thinking regardless.
